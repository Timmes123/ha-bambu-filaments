# HA-Bambu-Filaments

Custom Home Assistant integration ("Bambu Filaments", domain `bambu_filaments`) that surfaces the Bambu Lab cloud Filament Manager (spool inventory from Bambu Studio 2.6.1+/Handy) in Home Assistant. Will be published on GitHub (account: **Timmes123**, repo `ha-bambu-filaments`, public, MIT) and installed via HACS as a custom repository.

Authoritative documents:
- `RESEARCH.md` — API research (endpoints, auth, schemas, risks). Factual basis; verified 2026-08-27.
- `SPEC.md` — agreed design (architecture, entities, phases). Keep updated when decisions change.

## Project status (2026-08-27)

- Research complete: cloud endpoints under `https://api.bambulab.com/v1/design-user-service/my/filament/v2` confirmed.
- Design agreed: standalone project, write support planned, per-spool entities toggleable via options flow.
- **Phase 0 PASSED (2026-08-27)**: read AND write verified against the user's real account via `tools/phase0_verify.py` (~34 spools; full-object PUT works; `/filament/config` needs auth despite Studio source comment). Token cache `tools/.bambu_token.json` is local/gitignored.
- **Phases 1+2 SHIPPED (2026-08-27)**: repo live at github.com/Timmes123/ha-bambu-filaments, CI (hassfest + HACS action) green on main. Releases: v0.1.0 (read-only MVP) and v0.2.0 (adds `set_remaining`/`set_note` actions), both with `bambu_filaments.zip` asset (zip_release flow). Brand icons in `custom_components/bambu_filaments/brand/` + repo topics were required to pass the HACS action. Import smoke test passed against homeassistant==2026.8.3 in the local `.venv`.
- **LIVE and validated (2026-08-27, v0.3.1)**: installed via HACS on the user's HA, account configured by the user, 34 spools synced. v0.3.0 redesigned the model per user request: one HA device per spool (via_device -> "Bambu Filament Library" hub) with remaining-%/remaining-weight sensors and a delete-from-cloud button; official webshop color names (localized, via runtime-fetched `filaments_color_codes.json` from the BambuStudio repo, cached 7 days in HA storage); actions create_spool/delete_spool/set_remaining/set_note/refresh. Create/delete round trip tested live (create via action → device appeared → deleted via button → device gone). Login double-email fixed in v0.3.0 (Bambu auto-sends a code on the login attempt; never request another automatically).
- Gotchas learned: create body needs `#RRGGBB` color WITHOUT alpha and no colors/note/status fields (400 otherwise); never rename unique_ids between releases (v0.3.0 duplicated the total sensor, fixed in v0.3.1); per-spool cleanup keys on the `{entry_id}-spool-<int>` unique_id prefix, so aggregate unique_ids must not match it; PS 5.1 native-arg quoting breaks `git commit -m` messages containing double quotes (tag then pointed at the wrong commit — re-tag with `git tag -f`).
- **v1.0.0 STABLE (2026-08-27)**: card group_by `product` (product line across all brands, "Sorte (alle Hersteller)"). Full 8-angle audit ran before release; all 10 confirmed findings fixed (CSS injection via color values → strict hex validation both sides; stale-device reconciliation now every sync incl. reload, device-level removal + name refresh, shared `entity.py` platform helper; login-401 vs token-401 separated; reauth wrong-account abort; AuthExpired in services/buttons starts reauth; minimal PUT instead of stale read-modify-write; `_json` wrapper; `_unrecorded_attributes` against recorder bloat; explicit `filament_id ""` respected; color-DB failures can't break setup; admin-only write services; card entity caching + platform-filtered map; catalog cached on coordinator). Accepted trade-offs: card DOM rebuild per data change; Lovelace resource API untouched. HACS default-store PR submitted 2026-08-27: https://github.com/hacs/default/pull/10387 — OPEN, ready-for-review, all checks green (single one-line commit on branch `add-bambu-filaments` of the shared fork Timmes123/default, which also carries better-todo's `add-ha-better-todo` / PR #10360). Bot gotchas learned: (1) it auto-CLOSES PRs whose body doesn't exactly match `.github/PULL_REQUEST_TEMPLATE.md` incl. the hidden `<!-- tid:... -->` marker and the Links section with release + HACS-action-job + hassfest-job links (first attempt #10385 died that way); (2) when the branch falls behind master it comments and converts the PR to DRAFT — fix: rebuild the branch on fresh master (shallow fetch, `git checkout -B add-bambu-filaments origin/master`, re-run the one-line insertion after `"timmaurice/skyline-webcams"`, commit, `git push -f fork`), then `gh pr ready 10387`; the local clone lives only in the session scratchpad, re-clone `hacs/default` shallow when needed; fork remote is `https://github.com/Timmes123/default.git`. `mergeStateStatus: BLOCKED` = normal waiting-for-review state. The home-assistant/brands repo is obsolete for HACS — repo-local `brand/` assets are the accepted way (confirmed by the user and by better-todo). Forum thread live: https://community.home-assistant.io/t/bambu-filaments-your-bambu-lab-cloud-filament-library-spools-colors-remaining-weight-in-ha/1022849 (linked as README badge + feedback section). **Rule: internal drafts and plans never go into the public repo — hand them to the user in chat.** (Two draft files briefly landed in `docs/` and were removed again; note they remain visible in the repo's git history unless the user wants a history rewrite.)
- **v1.6.1 (2026-09-03, prerelease, deployed on live HA with ALL options enabled: sync_ams_remaining, empty_on_removal_pct 5, deduct_print_usage; v1.6.0 had an unload TypeError - Debouncer.async_shutdown is a @callback, not awaitable)**: (1) `sync_ams_remaining` - AMS remain% of known RFID spools pushed via `ams/sync` (server updates weight of known RFIDs, verified; 0 g -> `depleted` true), Studio rules from AmsAutoPushThrottle.cpp: skip unchanged, 10-min cooldown per spool only while printer busy (print_status running/pause/prepare), immediate when idle or 0 %. (2) `empty_on_removal_pct` - uuid gone from mounted set with last remain <= pct -> minimal PUT netWeight 0. (3) `deduct_print_usage` - cloud `GET /v1/user-service/my/tasks?limit=30` (all printers; status 2 finished, 3 failed/cancelled, 4 running) `amsDetailMapping[]` grams per slot; **amsId/slotId in the mapping are ALWAYS 0 - the real position is the flat `ams` index (unit*4+tray, HT 16-23)**; booked against the manual (RFID "") spool with inPrinter+devId+amsId+slotId, filamentId mismatch guard, foreign-change guard (net dropped by ~grams since previous sync), persistent done-list + enable timestamp in HA Store `bambu_filaments.usage_<entry_id>` (disable clears the window -> re-enable never books old jobs), cancelled jobs proportional via progress snapshot taken on print_status running->other. VERIFIED: nobody else books consumption for manual spools (Studio fila_manager has no deduction code; user's Anycubic stayed 267 g through a 64 g print; the RESEARCH.md "slicer-consumption deduction" claim was wrong). Coordinator subscribes to ha-bambulab tray + print_status sensors (async_track_state_change_event, 20 s Debouncer -> request_refresh); listener re-evaluated after every update. Options flow: AMS-dependent fields greyed via read_only selectors (`*_unavailable` keys) without a loaded bambu_lab entry; deduct option always available. Real-world tests still pending: new RFID spool insert (auto-register), a runout, and a print on the Anycubic (usage booking) - the user reports back; stable release only on their go.
- **v1.5.0 (2026-09-03, prerelease, deployed on live HA, option ENABLED on the user's entry; awaiting the user's real-world test = insert a new RFID spool and see it appear without opening Handy/Studio)**: auto-register RFID spools loaded into the AMS. Options toggle `auto_register_ams` (default off); when no `bambu_lab` config entry is LOADED the options flow shows the read-only placeholder key `auto_register_ams_unavailable` (BooleanSelector read_only -> greyed out by the HA config-flow form, value not submitted; verified in frontend step-flow-form.ts) whose data_description carries the ha-bambulab link via description_placeholders. Module `ams_register.py`: scans ha-bambulab AMS tray sensors (entity-registry platform bambu_lab, unique_id `{devtype}_{serial}_AMS_{amsSerial}_tray_{n}`; amsId from the AMS device name suffix `_AMS_{n}` -> n-1 (HT keeps 128+); printer name from via_device), keeps only trays with 32-hex non-zero tray_uuid + non-zero tag_uid + filament_id; external-spool sensors deliberately ignored. **Cloud facts verified live: the plain create endpoint IGNORES position fields (inPrinter/devId/...) and does NOT reject duplicate RFIDs; `POST /my/filament/v2/ams/sync` `{devId, items:[{RFID, createType, filamentVendor/Type/Name/Id, isSupport (REQUIRED), color, colorType, colors, rolls, netWeight, totalNetWeight, note, amsSn, slotId, amsId, amsType, trayIdName}]}` CREATES unknown RFIDs (`createdRFIDs` in response), sets position + cloud-side deviceName (server knows the printer name from devId) and leaves other slot mappings untouched; `slot-mappings/sync` returned code 3 for an RFID spool (that endpoint is for manual spools only, per Studio source).** trayIdName = filamentId[2:] + "-" + color DB `color_code` (e.g. G00-G01, matches Handy) via new `BambuColorDB.tray_code()`. Coordinator runs register BEFORE dedup so auto-registered spools consume pre-entered manual stock; in-memory sets `_registered_uuids` / `_skip_uuids` (uuid was mounted+known, then vanished from library while still mounted = user deleted it -> never resurrect until unloaded) / `_mounted_known`. Studio's own logic (wgtFilaManagerSync.cpp) only shows a "new filament" hint and creates on user click; it checks its local store by tray uuid first, so our pre-registered spool will not be duplicated by Studio. Studio sources cached in the session scratchpad only.
- **v1.4.0 (2026-09-03, prerelease, deployed+E2E-verified on live HA)**: multi-create ("stock"): `create_spool` field `count` (1-50, loops the POST, partial failure reports created-of-count, note written to every spool of the batch); card add dialog "Count" field; edit dialog (cog) section "Add more spools of this type" (uses the spool's STORED values, full copies, no display_name/note so auto-dedup consumes them first). Live test: count 2 + note -> 2 manual spools with note -> deleted via delete_spool. User declined a plus-icon shortcut on ×n stack chips. **VERIFIED via live probe: library `RFID` == ha-bambulab AMS slot sensor attribute `tray_uuid` (32 hex; `tag_uid` is 16 hex and NOT used)**; sensors `sensor.<printer>_ams_<n>_slot_<k>` carry tray_uuid/filament_id/name/type/color(#RRGGBBAA)/tray_weight/remain(%)/empty. Third-party spools have tray_uuid all-zero. Gap the user wants closed next (proposal pending their go): opt-in "auto-register new RFID spools" — compare tray_uuid of loaded slots against library RFIDs and create missing ones with createType "ams" + RFID (server requires the RFID field for ams creates); the Bambu apps only register a new spool when someone opens the device page.
- **v1.3.0 STABLE (2026-08-29, user-approved, marked --latest)**: card location now Studio-identical: `A2L · AMS 2 Pro · A1` (printer · AMS model · slot). Mapping VERIFIED in BambuStudio source (`formatSlotLocation` in device_page constants.ts, `DevAms::GetTrayId`, `GUI_App::transition_tridid`): slot letter = `'A'+amsId` (2nd AMS = B1–B4 — user's guess confirmed), number = slotId+1; AMS HT (amsType 4, amsId 128+) = `HT-A`; slotId 255/amsType 0 = external ("Extern"/"External"); AMS_TYPE_NAMES {1:AMS,2:AMS Lite,3:AMS 2 Pro,4:AMS HT,5:AMS Lite,6:AMS,7:AMS}. Cloud delivers `amsType` per spool (user's both AMS = 3); new sensor attribute `ams_type` (-1 mock for unmounted; card only reads it when in_printer). show_type labels renamed: `ams`→**RFID** (tag-registered ≠ sitting in AMS), rendered as `.pill` badge at the START of the meta line (trailing position got ellipsized away); mixed stacks = one pill per type. Card `_locations` removed from _combine (locations now derived from `_spools` at render time, needs translated ext label). "loaded" screenshot shows the pills. Deployed+verified on live HA.
- **v1.2.1 STABLE (2026-08-28, user-approved, marked --latest; bundles v1.1.1+v1.2.0)**: card option `show_type` (default off, "Spulentyp anzeigen (AMS / manuell)"): meta line shows each spool's `create_type` (AMS = tag-registered, manuell = hand-created; unknown future values pass through verbatim); combined stacks show the mix ("AMS ×5, manuell ×2"), uniform stacks just the label. Only createType values ever observed live: `ams` and `manual` (early RESEARCH.md mention of an "rfid" entry method never seen). Deployed+verified on live HA.
- **v1.2.0 (2026-08-28, prerelease)**: NEW auto-dedup feature (user idea: pre-register sealed official spools manually, integration reconciles them): options toggle `auto_dedup_manual` (default off, ENABLED on the user's live entry) — on each sync, for every AMS spool that is NEW vs the previous coordinator data, exactly one matching manual spool (same vendor+filamentId+color-hex6, full: net>=total>0) is deleted from the cloud; candidates without note/displayName go first, then lowest id; first refresh after (re)load only sets the baseline and never deletes. E2E-tested live: fake `createType:"ams"` probe spool via API (server REQUIRES an `RFID` field for ams creates — 400 otherwise) + full manual twin → refresh action → manual twin auto-deleted, log INFO written. Removed the dead `include_inactive` option + `spool_is_active()` entirely (status always 0; option did nothing). README: new "Track unopened stock — without duplicates" section between Features and Installation + features bullet (user wants this marketed prominently as better-than-Bambu, like the third-party-brands feature). The live HA options were rewritten via ha_set_integration (options flow works over MCP).
- **v1.1.1 (2026-08-28, prerelease)**: "Misty Blue discrepancy" investigated via live API (user saw 5 in Handy vs 7 in Studio): NOT a bug — cloud has exactly 7 (2 manual OVP, 3 in AMS, 2 with 0 g); Handy hides `depleted:true` spools, Studio shows all. Verified by self-cleaning probe spool: `depleted` is SERVER-DERIVED from `netWeight == 0` (PUT net 100 → false, net 0 → true; PUT `depleted:true` at net 100 forces netWeight to 0 — it's just another spelling of 0 g). `status` is read-only: PUT `status:1` is silently ignored, all spools always 0 → "archived" doesn't exist in the cloud (yet). NEW server validation: create now REQUIRES the `createType` field too (400 "field createType is not set"); our create_spool always sent `"manual"`, so unaffected. Card changes: `show_archived` option removed entirely (re-add if Bambu ever ships archiving; integration option `include_inactive` keys on the same dead status field and was left untouched pending user decision); `show_empty` off now filters BEFORE combine, so stacks shrink to their non-empty members (matches the apps). AMS-vs-manual finding: all 16 manual spools have RFID "", all 50 AMS-created ones have a 32-hex RFID — loading an official spool into the AMS will create a NEW spool keyed by its RFID tag rather than adopting a manual entry (manual pre-creation of official spools = future duplicates). User's library is at 66 spools now.
- **v1.1.0 STABLE (2026-08-27)**: user-approved stable bundling v1.0.1-v1.0.4 plus: `create_spool` accepts `note` (create endpoint 400s on note → service re-fetches after POST, picks newest unknown id matching vendor+name, writes note via minimal PUT — E2E-tested live via the HA action incl. cleanup); note field now in the card's CREATE dialog too. New README screenshots: purple-blue #4E00AD instead of green, harness has a Flashforge "Burnt Titanium" demo spool (GFL99, note filled), edit-dialog shot targets it; new Studio proof image (clipboard grab #2) shows the note as Studio's "Bemerkung" line. Studio's Bemerkung = the cloud `note` field (user confirmed). The user's real Burnt Titanium spool now has filament_id GFL99 + note set (they did it via the edit dialog). Release marked --latest; tag was force-moved once while still a draft (delete draft → tag -f → recreate). Deployed+verified on live HA (35 spools).
- **v1.0.4 (2026-08-27, prerelease)**: dialog no longer closes on outside clicks (text-selection drag released over the overlay fired a click there and closed it mid-edit; same keep-open behavior as ha-better-todo — only Cancel/save/delete close). Deployed+verified on live HA.
- **v1.0.3 (2026-08-27, prerelease)**: USER-CONFIRMED SUCCESS: a custom Flashforge spool with linked Generic-PLA profile shows up in Studio's AMS picker under its own brand "Flashforge (1)" and is assignable — screenshot saved as `images/studio-assign.png` (recovered from the user's clipboard via an STA PowerShell script) and embedded in the README. combine-stacks now expand on click into the individual spools (emptiest first, each with its own cog; `_key`/`_spools` on the aggregate, `_expandedStacks` set); row-click more-info popup removed entirely per user request (felt like a bug). Deployed+verified on live HA. **Pending: user wants to test v1.0.1–v1.0.3 and then explicitly approve a stable release (do NOT ship stable without their go).**
- **v1.0.2 (2026-08-27, prerelease)**: in-card edit dialog. New admin action `update_spool` (spool_id + any of vendor/material/name/color/total_g/remaining_g/note/filament_id/display_name; minimal PUT, color also rewrites the `colors` array — PUT accepts it although create 400s on it, all verified by probes). Card option `show_delete` replaced by `show_edit` (default on): cog icon per row opens the create dialog in edit mode (official spools preselect their catalog entry via vendor+filament_id match, others open in custom mode prefilled, profile preselected); save sends only changed fields; delete-from-cloud moved into the dialog. Screenshot harness got the cog glyph + PETG Basic/PLA Silk+ mock catalog entries + card-edit-dialog.png. Deployed+verified on live HA (all 8 services registered, no log errors).
- **v1.0.1 (2026-08-27, prerelease)**: slicer-profile linking for custom spools. Verified live: a minimal PUT sets/clears `filamentId` retroactively without touching vendor/name/displayName/color (probe GFA00 round trip). New admin action `set_filament_id` (spool_id, filament_id; "" unlinks); card add dialog's custom-brand mode gained a slicer-profile dropdown (catalog-fed, option values = catalog indices, auto-defaults to the Generic profile matching the typed material until manually changed). Deployed+verified on live HA (35 spools, service registered, no log errors). Open: user still needs to pick a profile for their existing "burnt titanium" spool and confirm Studio then allows assignment.
- **v0.9.1 (2026-08-27)**: Bambu apps sometimes persist a localized creation stamp ("07/29/2026 10:03 hinzugefügt") as displayName (Handy shows such labels for all spools, the cloud stores it once a spool is saved there; Studio's UI ignores the field). `clean_display_name()` treats names containing both a date and a time pattern as no-name → product+color naming and combining resume. Verified live.
- **v0.9.0 (2026-08-27)**: custom/third-party brands in the add dialog. Server limits mapped empirically (probes created+deleted): the ONLY create validation is that the `filamentId` FIELD exists — empty string OK (Studio's "non-official spool" model), garbage ids accepted, vendor/material/name are unvalidated free text; category derived from filamentType. The v0.3.0 400 was the missing filamentId (not the color alpha). Dialog vendor dropdown gained "Benutzerdefiniert/Fremdmarke…" → free-text brand/material(+datalist)/product line, sent with filamentId ""; create_spool always includes the filamentId key now.
- **v0.8.0 (2026-08-27)**: verified against live API — the cloud only accepts vendor/product combos from `/filament/config` (6 vendors: Bambu Lab, Generic, Overture, Polymaker, SUNLU, eSUN; free text → 400), and custom spool names live in the `displayName` field (found via the user's Handy-created "burnt titanium" spool). Add dialog now uses catalog dropdowns fed by the new `get_catalog` action (SupportsResponse.ONLY, 1 h cache on the coordinator) + optional custom-name field; `displayName` mapped into names/attributes/combine key; `create_spool` accepts `display_name`. Verified live: "burnt titanium" now shows as device name.
- **v0.7.0 (2026-08-27)**: in-card add-spool dialog (option `show_add`, default on; vendor/material datalists from the live library, native color picker; saves via create_spool action; card re-renders pause while the dialog is open). RFID roadmap item removed per user decision.
- **v0.5.0–v0.6.1 (2026-08-27)**: card gained `combine` (merge identical spools, ×n chip, summed remaining; normalizes cloud colors that arrive without a leading `#` — AMS-created spools do that), a Filters section (`max_remaining_g`, `max_remaining_pct`, `materials` checkboxes from live inventory, `only_in_printer`, `max_items`), and README screenshots. Screenshots are rendered headless: HTML harness with mock hass + minimal ha-card/ha-icon stand-ins in the scratchpad (`shots/`), element-screenshots via Playwright/Chromium in `.venv` into `images/`, embedded with absolute raw.githubusercontent URLs (HACS renders the installed tag's README). Live dashboard's "Fast leer" card uses `combine` + `max_remaining_g: 200`. RFID/AMS linkage deemed redundant (cloud already delivers per-spool mount location) — dropped unless the user asks for live-MQTT coupling.
- **v0.4.0 (2026-08-27)**: ships `custom:bambu-filaments-card` (vanilla web component in `www/`, served via `frontend.py` + auto-registered Lovelace resource with `?v=<version>`; manifest needs `dependencies: ["http", "lovelace"]` for hassfest). Card reads the aggregate spools sensor attributes (auto-discovered), DE/EN, visual editor; options: group_by line/material/none, sort, show_empty/archived/location/code/note/delete, compact, thresholds, max_height. New integration option `color_language` (auto/de/en) — Bambu's color DB has empty `de` names for several colors, fallback is EN (same as Studio). Test dashboard "Filamente" (`/bambu-filamente/spulen`) created on the live HA with two card variants. README got My-HA install/setup badges.
- Next ideas (user roadmap): AMS-RFID linkage to ha-bambulab tray sensors.
- The user's live HA is reachable via MCP; the Bambu printer integration (cloud mode) is installed there, printers: A1 Mini + A2L, each with AMS.

## Release policy (since v1.0.0)

**All releases after v1.0.0 are GitHub prereleases** (`gh release create --prerelease`), exactly like ha-better-todo — UNLESS the user explicitly asks for a stable release. HACS installs prereleases fine when downloading a pinned version. Version numbering continues normally (1.0.1, 1.1.0, …).

## Hard rule: deployment only via HACS releases (same as ha-better-todo)

No direct file deployment into the live Home Assistant (`ha_write_file` for integration code is forbidden). Develop locally → commit → bump `manifest.json` → tagged GitHub release (`gh` CLI at `C:\Program Files\GitHub CLI\gh.exe`, not on PATH) → update via HACS → restart HA → verify via MCP (read-only). Once the repo exists, mirror ha-better-todo's release setup (zip_release, draft-with-asset flow).

## Security rules for this project

- Never ask for, read, echo, or store the user's Bambu password/tokens in chat, files tracked by git, or logs. The Phase-0 script prompts interactively; the token cache stays local and gitignored.
- Diagnostics in the integration must redact tokens, RFIDs, device serials.

## Dev environment & runbook (for future sessions)

- Local `.venv` (Python 3.14) has: curl_cffi, pillow, playwright (+ chromium), homeassistant==2026.8.3 for import smoke tests. Node 24 available for JS syntax checks (`node -e "new Function(require('fs').readFileSync(...))"`).
- **Release pipeline** (every change): bump `manifest.json` version (BOM-free write!) → commit/push → wait for CI green → `git archive -o bambu_filaments.zip <tag>:custom_components/bambu_filaments` → `gh release create <tag> bambu_filaments.zip --draft [--prerelease]` → `gh release edit <tag> --draft=false` → delete local zip (it is gitignored, but one slipped into a commit once) → `ha_manage_hacs update_information` + `download version=<tag>` (repo id 1348396125) → `ha_restart` (the MCP "connection lost" error on restart is EXPECTED and means it worked) → wait ~2 min → verify via `ha_get_logs`/`ha_search`.
- **Screenshots**: `python tools/screenshots/take_shots.py` (harness.html mocks hass + ha-card/ha-icon; Playwright element shots into `images/`). Re-run whenever card visuals change; README embeds absolute raw.githubusercontent URLs.
- **API probes**: `tools/.bambu_token.json` (gitignored) caches the user's ~90-day cloud token from phase 0 — reusable for read probes and self-cleaning write probes with curl_cffi (`impersonate="chrome"`). Never handle the user's password; they run interactive logins themselves.
- **PowerShell 5.1 traps**: double quotes INSIDE here-string arguments to native exes (git -m, gh --notes, python -c) get mangled and split the argument — write multi-line python to a script file instead, and keep commit messages/release notes free of `"`. A failed `git commit` followed by `git tag` pushes a tag pointing at the OLD commit — fix with `git tag -f` + `git push -f origin <tag>`. `Set-Content -Encoding utf8` writes a BOM (hassfest rejects it) — use `[System.IO.File]::WriteAllText` with `UTF8Encoding($false)`.
- Live HA facts: printers A1 Mini + A2L (each with AMS) via the Bambu printer integration (cloud mode); our config entry logged in by the user; dashboard "Filamente" at `/bambu-filamente/spulen` (two card variants, combine on, low-stock filter 200 g); HA auto-mode classifier may block HACS/dashboard write tools — if denied twice, stop and ask the user instead of working around.

## Conventions

- User speaks German — respond in German. Repo files (README, code comments, docs) in English.
- HA timezone: Europe/Berlin.
- Windows/PowerShell 5.1 gotcha (from ha-better-todo): write UTF-8 files without BOM — use `[System.IO.File]::WriteAllText` with `UTF8Encoding($false)`, not `Set-Content -Encoding utf8`; hassfest rejects BOMs.
- README must state clearly: unofficial reverse-engineered cloud API, requires Bambu cloud account (no LAN mode), ~90-day re-login, may break at any time.
